# Google Integration

## Status

- Chưa có tích hợp Google API ngoài Gemini.
- `lib/apikeys.ts` còn tham chiếu `GOOGLE_API_KEY` (legacy).

## Action

- Nếu cần dùng Google APIs khác, cập nhật docs + verify script.
